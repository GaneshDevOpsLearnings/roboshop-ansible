- name: Create Parameters
  hosts: localhost
  connection: local
  tasks:
    - name: Create Parameters
      community.aws.aws_ssm_parameter_store:
        name: "{{ item.name }}"
        value: "{{ item.value }}"
        region: us-east-1
      loop:
        - {name: "dev.frontend.catalogue" , value: "1"}
        - {name: "dev.frontend.user" , value: "1"}
        - {name: "dev.frontend.cart" , value: "1"}
        - {name: "dev.frontend.shipping" , value: "1"}
        - {name: "dev.frontend.payment" , value: "1"}